/**
 * Pagination — applyPagination for skip/take on result arrays.
 * Implemented in prompt 6.4.
 */

import type { RawRecord } from "../persistence/index.js";

/**
 * Applies skip and take to a result array.
 * Stub — not yet implemented.
 */
export const applyPagination = (
  records: RawRecord[],
  _skip: number | undefined,
  _take: number | undefined,
): RawRecord[] => records;
