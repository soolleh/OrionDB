/**
 * Sort compiler — orderBy single and multi-field sorting.
 * Implemented in prompt 6.3.
 */

import type { CompiledSort, OrderByInput } from "./types.js";

/**
 * Compiles an OrderByInput into a CompiledSort comparator.
 * Stub — not yet implemented.
 */
export const compileSort = (_orderBy: OrderByInput | undefined): CompiledSort | undefined => undefined;
